
import styles from "./PricingTrail.module.css";
import React from "react";
import Image from "next/image";
export const PricingTrail = (props) => {
    return (
        <div>
            {props.data}
           
        </div>
    );
}